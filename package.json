{
  "name": "idizr",
  "version": "1.0.2",
  "description": "add unique ids to a javascript object",
  "main": "src/index.js",
  "repository": "git@github.com:nelsonomuto/idizr.git",
  "author": "Nelson Omuto <nelsonomuto@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.26.3",
    "babel-jest": "^23.4.2",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.4.2",
    "rimraf": "^2.6.2",
    "rollup": "^0.63.5"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && NODE_ENV=production rollup -c",
    "test": "NODE_ENV=test jest",
    "test:debug": "NODE_ENV=test npx -n=--inspect jest --watch"
  }
}
